const initModel = {
  leftValue: 32,
  leftUnit: 'Fahrenheit',
  rightValue: 0,
  rightUnit: 'Celsius',
  sourceLeft: true
};

export const Side = {
  LEFT: 'left',
  RIGHT: 'right'
};

export const UNITS = ['Fahrenheit', 'Celsius', 'Kelvin'];

export default initModel;